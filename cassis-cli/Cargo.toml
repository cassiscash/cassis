[package]
name = "cassis-cli"
version.workspace = true
edition.workspace = true

[features]
default = []
cashu = ["dep:cassis-cashu", "dep:cdk", "cassis-router/cashu", "cassis-core/cashu"]
fedimint = ["dep:cassis-fedimint"]
ark = ["dep:cassis-arkade"]
liquid = ["dep:cassis-liquid"]
rootstock = ["dep:cassis-rootstock", "cassis-router/rootstock", "cassis-core/rootstock"]

[dependencies]
cassis-client = { path = "../cassis-client" }
cassis-core = { path = "../cassis-core" }
cassis-iroh = { path = "../cassis-iroh" }
cassis-keys = { path = "../cassis-keys" }
cassis-router = { path = "../cassis-router" }
cassis-routing = { path = "../cassis-routing" }
clap = { version = "4.6.6", features = ["derive"] }
rand = "0.8.7"
sha2 = "0.10.9"
futures = "0.3.33"
iroh = "1.0.3"
lowercase-hex = "0.1.1"
minisqlite = { git = "https://github.com/cursor/minisqlite" }
rustls = "0.23.43"
serde_json = "1.0.151"
thiserror = "1.0.69"
tokio = { version = "1.53.1", features = ["rt-multi-thread", "macros", "sync", "signal", "time"] }
base64 = "0.22.1"
tracing = "0.1.44"

[dependencies.cassis-cashu]
path = "../networks/cassis-cashu"
optional = true

# Used by the cashu wallet subcommands to encode / decode
# NUT-00 tokens via `cdk::nuts::Token`. Same version
# `cassis-cashu` pulls in transitively.
[dependencies.cdk]
version = "0.17.3"
features = ["wallet"]
optional = true

[dependencies.cassis-fedimint]
path = "../networks/cassis-fedimint"
optional = true

[dependencies.cassis-arkade]
path = "../networks/cassis-arkade"
optional = true

[dependencies.cassis-liquid]
path = "../networks/cassis-liquid"
optional = true

[dependencies.cassis-rootstock]
path = "../networks/cassis-rootstock"
optional = true
